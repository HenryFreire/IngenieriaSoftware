public class ordenamiento  {
    dibujar dibujarFigura = new dibujar();   
    public Array ordenar (Figuras[] formas, String tipoOrden){
       if(tipoOrden === "desc") {
            for(int i = formas.size() - 1; i > 0 i--){
                 dibujarFigura.dibujar(formas[i]);   
            }
       }else{
           for(int i =0; i < formas.size(); i++){
                 dibujarFigura.dibujar(formas[i]);   
            }
       }
        
    }

}
